services:
  postgres:
    image: postgres:17-alpine
    container_name: ozon-postgres
    ports:
      - "5440:5432"
    environment:
      POSTGRES_USER: ozon
      POSTGRES_PASSWORD: ozon
      POSTGRES_DB: ozon
    volumes:
      - ozon-pgdata:/var/lib/postgresql/data

volumes:
  ozon-pgdata: